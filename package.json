{
  "name": "employee_tracker",
  "version": "1.0.0",
  "description": "A Command line application that will provide a tabled representation user with prompts to answer so that it can generate a structure by answering the prompts.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^8.2.4",
    "mysql2": "^2.2.5",
    "console.table": "^0.10.0"
  }
}
